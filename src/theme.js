import { createTheme } from '@material-ui/core'

const theme = createTheme({
    direction: 'rtl',
    palette: {
      primary: {
        main: '#455a64'
      },
      secondary: {
        main: '#f44336'
      },
      white: '#FFFFFF'
    }
})

export default theme